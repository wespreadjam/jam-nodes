{
  "name": "jam-nodes-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Extensible workflow node framework for building automation pipelines",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "tsx test.ts",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "playground": "npm run dev --workspace=@jam-nodes/playground",
    "playground:cli": "tsx packages/playground/src/index.ts",
    "playground:web": "npm run dev --workspace=@jam-nodes/playground-web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wespreadjam/jam-nodes.git"
  },
  "keywords": [
    "workflow",
    "nodes",
    "automation",
    "pipeline",
    "zod"
  ],
  "author": "Jam",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wespreadjam/jam-nodes/issues"
  },
  "homepage": "https://github.com/wespreadjam/jam-nodes#readme",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "@jam-nodes/core": "^0.2.5",
    "@jam-nodes/nodes": "^0.2.5",
    "zod": "^4.3.6"
  }
}
